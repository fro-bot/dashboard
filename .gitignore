node_modules
.env
coverage
dist
web/dist
.DS_Store

# Never commit private keys (App PEM, cookie key). Defense-in-depth: do not
# rely on a machine-global gitignore for a public repo holding a read credential.
*.pem
*.key

# BEGIN oh-my-opencode-slim clonedeps
.slim/clonedeps/repos/
# END oh-my-opencode-slim clonedeps
.context/

# Opencode
!.opencode/
.opencode/*
!.opencode/themes/
!.opencode/tui.json
