# Fork security workflow validation
This temporary change verifies pull requests from forks run read-only Dependency Review, CodeQL, existing CI, and Fro Bot checks without repository secrets.

Do not merge.
